version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/frontend
    overwrite: yes

permissions:
  - object: /home/ubuntu/frontend
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: deploy.sh
      timeout: 60
      runas: ubuntu
