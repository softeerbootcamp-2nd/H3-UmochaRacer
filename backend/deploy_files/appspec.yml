version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/backend
    overwrite: yes

permissions:
  - object: /home/ubuntu/backend
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: deploy.sh
      timeout: 60
      runas: ubuntu
